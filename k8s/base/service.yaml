apiVersion: v1
kind: Service
metadata:
  name: $DEPLOYMENT_NAME-service
  namespace: $NAMESPACE_NAME
  labels:
    app: $APP_NAME
spec:
  type: NodePort
  selector:
    app: $APP_NAME
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30090